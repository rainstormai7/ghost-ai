# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Phase 2: Canvas & collaboration — foundation (Prisma, project APIs, editor home wiring) is in place.

## Current Goal

- Extend collaborative canvas with controls, custom node shapes/colors from `types/canvas.ts`, persistence, and AI-driven graph edits (per roadmap).

## Completed

- `01-design-system`: shadcn/ui initialized with Tailwind v4 support; Button, Card, Dialog, Input, Tabs, Textarea, and ScrollArea components added to `components/ui/`; lucide-react installed; `lib/utils.ts` created with `cn()` helper; dark theme CSS custom properties defined in `app/globals.css` with project design tokens mapped to Tailwind utilities via `@theme inline`.
- `02-editor`: `components/editor/editor-navbar.tsx` — fixed top navbar with sidebar toggle (PanelLeftOpen / PanelLeftClose); `components/editor/project-sidebar.tsx` — floating overlay sidebar with Projects header, My Projects / Shared tabs (empty placeholder state), and New Project button; dialog color-token pattern documented in globals.css for future use.
- `03-auth`: `@clerk/ui` installed; `proxy.ts` at project root with `clerkMiddleware()` protecting all routes except `/sign-in` and `/sign-up`; `ClerkProvider` with Clerk `dark` theme wraps root layout; Clerk CSS variables overridden via `globals.css` using project design tokens (no hardcoded colors); `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` with two-panel layout (left: logo + tagline + feature list, right: Clerk form) on large screens, form-only on small screens; `app/page.tsx` redirects authenticated users to `/editor` and unauthenticated users to `/sign-in`; `UserButton` added to editor navbar right section; `app/editor/page.tsx` shell created. Auth marketing shell: `components/auth/auth-split-shell.tsx` + `auth-marketing-aside.tsx` — 50/50 `lg` split (`bg-surface` left vs `bg-base` right), headline + feature rows with Lucide icons; Geist Sans via `body`/shell `font-sans`, Clerk `--clerk-font-family*` in `globals.css`, and `appearance.elements` on `ClerkProvider` / sign-in/up.
- `04-project-dialogues`: `useProjectDialogs` hook (`hooks/use-project-dialogs.ts`) for dialog, form, and loading state; `ProjectDialogs` + slug helper `lib/project-slug.ts` and mock data `lib/mock-projects.ts`; `/editor` home heading, description, and `New Project` (Plus); create (name + live slug preview), rename (prefill, current name in description, auto-focus, Enter submits), delete (confirmation, destructive confirm); sidebar lists mock owned vs shared projects, rename/delete only for owners; mobile-only backdrop scrim closes sidebar on tap; `Input` forwards refs for focus.
- `05-prisma`: `prisma/models/project.prisma` — `Project` (ownerId, name, optional description, `ProjectStatus` DRAFT/ARCHIVED, optional `canvasJsonPath`, timestamps; indexes on `ownerId`, `createdAt`) and `ProjectCollaborator` (project relation cascade delete, email, createdAt; `@@unique([projectId, email])`; indexes on email and `[projectId, createdAt]`). `lib/prisma.ts` — cached singleton (`globalThis` in non-production): `DATABASE_URL` `prisma+postgres://*` → Accelerate (`accelerateUrl` + `@prisma/extension-accelerate`); else `Pool` + `PrismaPg` adapter. Migration `20260502101516_init` applied; client generated under `app/generated/prisma`.
- `06-project-apis`: `lib/api-clerk.ts` — `requireClerkUserId()` returns 401 JSON for unauthenticated API callers. `GET/POST /api/projects` — list owner’s projects (`updatedAt` desc); create with Clerk `ownerId`, default name `Untitled Project`, status `DRAFT`, cuid from schema. `PATCH/DELETE /api/projects/[projectId]` — owner-only rename/delete; 404 if missing, 403 if non-owner; PATCH validates non-empty `name`. `lib/prisma.ts` — export typed as `InstanceType<typeof PrismaClient>` so Accelerate vs adapter clients unify for type checking (Accelerate branch cast via `unknown`).
- `07-wire-editor-home`: `lib/editor-project.ts` + `lib/editor-projects-data.ts` — server `getEditorProjectsForUser()` (owned + shared via collaborator email match). `app/editor/page.tsx` + workspace route — RSC fetch, pass lists into `EditorShell`. `hooks/use-project-actions.ts` — dialog state, room ID preview (`slugify` + short suffix), `POST/PATCH/DELETE` project APIs, `router.push` to new workspace (project id = room id), rename `router.refresh()`, delete refresh or `replace` home when active. `components/editor/editor-shell.tsx` — client chrome; `ProjectSidebar` uses real `EditorProject` rows with `Link` to workspace; `ProjectDialogs` room ID preview label. Removed `use-project-dialogs` (replaced by actions hook).
- `08-editor-workspace-shell`: `lib/project-access.ts` — `getClerkProjectIdentity()`, `findAccessibleProjectForUser(roomId, identity)` (owner or collaborator email). `components/editor/access-denied.tsx` — centered lock state + link to `/editor`. `app/editor/[roomId]/page.tsx` — server component: redirect unauthenticated to `/sign-in`; missing/forbidden project renders `AccessDenied` (not `notFound`). `components/editor/editor-workspace-shell.tsx` — full-viewport workspace: navbar with project title, workspace toolbar (share + AI toggle); `ProjectSidebar` with `activeProjectId` highlight; canvas placeholder + AI sidebar placeholder. `components/editor/editor-navbar.tsx` — optional workspace props; `components/editor/project-sidebar.tsx` — `activeProjectId` row highlight.
- `09-share-dialog`: Share from workspace navbar opens `ShareDialog`. `GET/POST /api/projects/[projectId]/collaborators` — list or invite (owner-only POST); `DELETE /api/projects/[projectId]/collaborators/[collaboratorId]` — owner-only remove. `getProjectShareAccess` in `lib/project-access.ts` — 404 unknown project / 403 no access / owner vs collaborator. `lib/clerk-collaborator-enrichment.ts` — Clerk Backend `users.getUserList({ emailAddress })` for display name + avatar, fallback to email. Owners: invite input, remove, copy link with **Copied!**; collaborators: read-only list + copy link.
- `10-liveblocks-setup`: Root `liveblocks.config.ts` — `Presence` (`cursor` nullable point + `isThinking`), `UserMeta` (`name`, `avatar`, `cursorColor`). `lib/liveblocks-node.ts` — cached `@liveblocks/node` client + `cursorColorFromUserId()`. `POST /api/liveblocks-auth` — Clerk session, `findAccessibleProjectForUser(room, identity)`, `getOrCreateRoom` with `defaultAccesses: []`, access token with Clerk name/avatar and deterministic cursor color; `403` if project inaccessible. Dependency: `@liveblocks/node` aligned with 3.18.x. Fix: collaborator DELETE route builds `ClerkProjectIdentity` (`verifiedEmails`) like GET.
- `11-base-canvas` (`context/feature-specs/11-base-canvas.md`): `types/canvas.ts` — `NODE_COLORS`, `NODE_SHAPES`, `CanvasNodeData` (`label`, `color`, `shape`), `CanvasNode` / `CanvasEdge` (`canvasNode` / `canvasEdge`). `liveblocks.config.ts` — optional Storage `flow` typed as `LiveblocksFlow<CanvasNode, CanvasEdge>`. `components/editor/editor-collaborative-canvas.tsx` — client wrapper: `LiveblocksProvider` (`/api/liveblocks-auth`), `RoomProvider` (`id` = room), `initialPresence` (`cursor: null`, `isThinking: false`), `ClientSideSuspense` loading stub, `useErrorListener` + reload CTA on `ROOM_CONNECTION_ERROR`; `useLiveblocksFlow({ suspense: true, initial [] nodes/edges })` driving `ReactFlow` with loose connections, `fitView`, dot `Background`, `MiniMap`, `Cursors`; `SmoothStepEdge` for `canvasEdge`. `editor-workspace-shell.tsx` swaps placeholder for live canvas (`roomId`). Workspace route stays server-driven.
- `12-shape-panel` (`context/feature-specs/12-shape-panel.md`): Bottom-center pill `EditorCanvasShapeToolbar` in a `Panel` with draggable Lucide-backed shape buttons; `CANVAS_SHAPE_DRAG_MIME` + `CanvasShapeDragPayload` / `CANVAS_SHAPE_DEFAULT_SIZE` / `parseCanvasShapeDragJson` in `types/canvas.ts`; `ReactFlow` `onDragOver` / `onDrop` + `onInit`-held `ReactFlowInstance` converts screen coords and `addNodes` as `canvasNode` with stable ids + default neutral fill + empty label (shape visuals in `13-node-shape`).
- `13-node-shape` (`context/feature-specs/13-node-shape.md`): `CanvasNodeShapeVisual` — CSS borders for `rectangle` / `pill` / `circle` (`--border-subtle` at rest, `--accent-primary` when selected); SVG `polygon` / `path` for `diamond` / `hexagon` / `cylinder` with viewBox scaling; `CanvasFlowNode` delegates to it. **Cylinder label** is clipped to the `meet` square and limited to **~56%** width (cylinder body in viewBox) with nested `@container-[size]` + `clamp()` font / `cqw` max-height so labels wrap and scale within the DB silhouette (edit overlay matches); not the full rectangular node. Palette drag ghost: `EditorCanvasShapeToolbar` `onPaletteDragStart` / `onPaletteDragEnd`, document `dragover` tracking, `createPortal` preview sized by flow zoom × default dimensions, cleared on drop/cancel. **Handles:** `CanvasNodeHandles` — stacked XYFlow target+source at cardinals (`in-*` / `out-*`); visible only when selected, node drag, or global connection-in-progress (`useConnection`). **Edges:** `onConnect` enriches params with `type: "canvasEdge"` + arrow marker + stroke before Liveblocks `addEdge`; `onConnectEnd` runs `lib/canvas-connection-snap.ts` (`trySnapConnectionOnPointerUp`) so a line dropped on/near a node snaps to the nearest valid handle + `connectionRadius` 48 for easier handle hits.
- `14-node-editing` (`context/feature-specs/14-node-editing.md`): **Resize:** XYFlow `NodeResizer` when selected; `CANVAS_NODE_MIN_SIZE` in `types/canvas.ts`. **Label:** centered placeholder for empty labels (`CANVAS_NODE_LABEL_PLACEHOLDER`); double-click body opens a `nodrag` / `nopan` / `nowheel` textarea overlay; `updateNodeData` on change for Liveblocks sync; blur or `Escape` closes; `labelHidden` on `CanvasNodeShapeVisual` while editing. Ghost palette preview unchanged (no placeholder copy).
- `15-nodes-color-toolbar` (`context/feature-specs/15-nodes-color-toolbar.md`): **`CanvasNodeColorToolbar`** — floating pill above selected nodes (`bottom-full`, gap below node); one swatch per `NODE_COLORS` pair from `types/canvas.ts`; `nodrag` / `nopan` / `nowheel` + `stopPropagation` on pointer down so toolbar doesn’t drag nodes or pan canvas; active swatch uses `ring-2` + paired label color; hover uses tight `box-shadow` via `--swatch-label`. **`CanvasFlowNode`** calls `updateNodeData(id, { color })` so fill updates immediately and label color follows via `canvasLabelColorForFill` (Liveblocks flow state only).
- `16-edge-behavior` (`context/feature-specs/16-edge-behavior.md`): **`CanvasLabeledEdge`** — `getSmoothStepPath` + dual paths (wide transparent interaction stroke + visible rounded stroke/arrow); dim at rest, bright on hover/selection. **`EdgeLabelRenderer`** uses path midpoint `labelX`/`labelY` from `getSmoothStepPath` only. Inline label editing: double‑click path, auto‑width `input`, commit on blur/Enter/Escape via `updateEdgeData`; saved labels as pills; faint hint when hovered/selected and empty; `nodrag`/`nopan`/`nowheel` on the field. **`CanvasEdgeData`** (`label`) + `defaultEdgeOptions` / `handleConnect` seed `data`. **`CanvasNodeHandles`** — hover‑revealed dots (dark border) + full opacity during connection drag.
- `17-canvas-ergonomics` (`context/feature-specs/17-canvas-ergonomics.md`): Bottom-left pill **`EditorCanvasViewToolbar`** in a `Panel` (`mb-14`, `ml-4`) — zoom out / fit view / zoom in (React Flow `zoomOut` / `fitView` / `zoomIn` with 250ms duration), thin divider, undo / redo (`useUndo` / `useRedo` / `useCanUndo` / `useCanRedo` from `@liveblocks/react/suspense`); disabled history buttons dimmed (`disabled:opacity-35`). **`hooks/useKeyboardShortcuts`** — `window` `keydown`: `+` / `=` zoom in, `-` zoom out (no modifiers); `Cmd/Ctrl+Z` undo, `Cmd/Ctrl+Shift+Z` / `Cmd/Ctrl+Y` redo; skips when focus path is `input` / `textarea` / `select` / `contentEditable`. Shape panel unchanged.
- `18-starter-template` (`context/feature-specs/18-starter-template.md`): **`components/editor/starter-templates.ts`** — `CanvasTemplate`, `CANVAS_TEMPLATES` (microservices, CI/CD pipeline, event-driven), helpers `templateNode` / `templateEdge` using `CanvasNode` / `CanvasEdge`, `NODE_COLORS`, and `MarkerType` defaults aligned with `handleConnect`. **`StarterTemplatesModal`** — dialog + scrollable grid, per-card previews, Import closes modal. **`EditorNavbar`** — workspace **Templates** control. **`EditorWorkspaceShell`** wires modal + `importFlowRef`. **`lib/canvas-template-merge.ts`** — places imported template **below** existing nodes (aligned to existing left edge, vertical gap), **remaps node/edge ids** to avoid collisions. **`EditorCollaborativeCanvas`** import merges via `onNodesChange`/`onEdgesChange` wrapped in `room.history.pause()` / `resume()` (`try`/`finally`) so the full import is one undo step; deselect prior selection; **imported nodes remain unselected** to avoid stacked color toolbars; `fitView({ nodes: imported })` after double rAF. No template saving, user templates, or server persistence.

## In Progress

- None.

## Next Up

- Blob persistence wiring, AI-driven graph edits, further canvas polish beyond ergonomics bar.

## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- Add decisions that affect the system design or data model.

## Session Notes

- Workspace editor: full-bleed canvas under navbar (`fixed inset-x-0 top-14 bottom-0 z-0`); Projects + AI panels are `fixed` floating cards (`z-40`, `top-[calc(3.5rem+0.75rem)]`, `bottom-3`, gutters) and unmount when toggled off. Shape palette enlarged with `border-2`, `gap-4/5`, prominent hover (brand border/fill + glow) and active dragging state.
- Auth UI (spacing + Clerk colors): Desktop uses `bg-surface` marketing column vs `bg-base` Clerk column; form column is `lg:justify-start` with padding matching the aside (`lg:px-14 lg:py-16`), inner wrapper `lg:mt-14` to align the card top with the headline under the logo row. Marketing aside: tighter logo→H1 (`mb-7`), subhead (`mt-4`), features (`mt-8` / `space-y-6`). Clerk: `:root` adds `--clerk-color-neutral`, `--clerk-color-ring`; `ClerkProvider` `appearance.variables` reinforce tokens; `elements.formButtonPrimary` and `footerActionLink` force brand primary + teal links so the CTA matches reference PNGs over theme defaults. **Follow-up:** Increased top breathing room — `lg:pt-24` on both columns, logo→headline `mb-10`, Clerk offset `lg:mt-17` to keep card aligned with H1; mobile form wrapper `py-12`.
